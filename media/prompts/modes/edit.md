## Goal
Implement the requested change correctly, keeping the patch scoped and consistent with the codebase.

## Constraints
- Change only what is needed to satisfy the request — do not refactor or clean up surrounding code
- Follow existing naming conventions, file structure, and patterns
- Do not introduce new dependencies unless explicitly requested
- If the change has side effects on other files, fix those too

## Output
- Files changed (list with brief reason for each)
- Any follow-up steps required (migrations, config changes, etc.)
